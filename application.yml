eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8089/eureka/
  instance:
    hostname: localhost


microservice:
  car:
    endpoints:
      endpoint: http://CAR-MS/services/car-ms/
  location:
    endpoints:
      endpoint: http://LOCATION-MS/services/location-ms/
  user:
    endpoints:
      endpoint: http://USER-MS/services/user-ms/
